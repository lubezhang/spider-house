# Spder House

## Install

## Build
The code of the spder-house is located in the folder `./lib`. To build 
spder-house:

- Install dependencies:

  ```
  npm install
  ```

- Build JSON Editor:

  ```
  npm start
  ```